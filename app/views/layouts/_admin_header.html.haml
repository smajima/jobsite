#a-header
  #a-header-inner
    = link_to image_tag('admin_title.png', { :border => '0', :alt => 'admin jobs'}), admins_companies_path
  #a-header-right
    - if admin_signed_in?
      = link_to "logout", destroy_admin_session_path, :method => :delete
  #a-header-lower
    = render :partial => 'admins/shared/header_menus'

