= render :partial => "/share/mypage_common"
%h2
  = "Registered Working History"
- @job_histories.each do |job_history|
  = render :partial => 'show_working_history', :locals => {:job_history => job_history}

= link_to "Edit", edit_resume_path

