#edit
  = "Edit Job"
  = form_for(@job, :url => admins_company_job_path(params[:company_id], @job.id)) do |f|
    = render :partial => "form", :locals => {:f => f}
    #job-submit
    %p
      = f.submit


